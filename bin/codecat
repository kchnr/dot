#!/bin/bash
while read -r file; do  # Change from "$@" to stdin reading
    if [ -d "$file" ]; then
        echo "cat $file"
    elif [ -f "$file" ]; then
        echo "==START FILE: $file"
        cat "$file"
        echo "==END FILE: $file"
    fi
done
